package CH1

import "fmt"

func L25() {
	fname := "Dalinar"
	lname := "Kholin"
	age := 45
	messageRate := 0.5
	isSubscribed := false
	message := "Sometimes a hypocrite is nothing more than a man in the process of changing."
	userLog := fmt.Sprintf("Name: %s %s, Age: %v, Rate: %v, Is Subscribed: %v, Message: %s", fname, lname, age, messageRate, isSubscribed, message)
	// Don't touch above this line

	// ?

	// Don't touch below this line

	fmt.Println(userLog)
}
